# Friendly_App

## installation

setup virtual environment:
```bash
$ python3 -m venv venv
$ . venv/bin/activate

$ pip install -r requirements.txt
```


## running server

```bash
$ flask --app friendly_app.py run --debug
```
